(function (window) {
	'use strict';

	!window.magnolia && (window.magnolia = {});

	window.magnolia.checked = checked

	function checked(){
		return !!(typeof window.callPhantom === 'function')
	}

}(window));
